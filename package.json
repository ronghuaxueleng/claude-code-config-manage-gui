{
  "name": "claude-config-manager",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node build.mjs",
    "build:debug": "node build.mjs --debug",
    "build:clean": "node build.mjs --clean",
    "build:msi": "node build.mjs --target=msi",
    "build:nsis": "node build.mjs --target=nsis",
    "build:help": "node build.mjs --help"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
